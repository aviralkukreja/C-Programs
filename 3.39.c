//Checkerboard Pattern of Astericks
//Problem 3.39
//By: Aviral Kukreja
//06/16/16
#include <stdio.h>

//Function main begins program execution
int main(void)
{
    unsigned int counter;//Number of times to put an asterick
    
    //initialization phase
    counter=1;//Initialize loop counter
    
    //processing phase
    while (counter<=8){//loop 8 times
        
        printf("%s", "* ");//Print asterick
        counter=counter+1;//increment counter
    }
    
    puts("");//outputs a new line
    printf("%s", " ");//outputs an extra space
           
    //initialization phase
    counter=1;//Initialize loop counter
    
    //processing phase
    while (counter<=8){//loop 8 times
        
        printf("%s", "* ");//print asterick
        counter=counter+1;//increment counter
    }
    
    puts("");//outputs a new line
    
    //initialization phase
    counter=1;//initialize loop counter
    
    //processing phase
    while (counter<=8){//loop 8 times
        
        printf("%s", "* ");//print asterick
        counter=counter+1;//increment counter
    }
    
    puts("");//outputs a new line
    printf("%s", " ");//outputs an extra space
    
    //initialization phase
    counter=1;//initialize loop counter
    
    //processing phase
    while (counter<=8){//loop 8 times
        
        printf("%s", "* ");//print asterick
        counter=counter+1;//increment counter
    }
    
    puts("");//outputs a new line
    
    //initialization phase
    counter=1;//initialize loop counter
    
    //processing phase
    while (counter<=8){//loop 8 times
        
        printf("%s", "* ");//print asterick
        counter=counter+1;//increment counter
    }
    
    puts("");//outputs a new line
    printf("%s", " ");//outputs an extra space
    
    //Initialization phase
    counter=1;//Initialize loop counter
    
    //processing phase
    while (counter<=8){//loop 8 times
        
        printf("%s", "* ");//print asterick
        counter=counter+1;//increment counter
    }
    
    puts("");//outputs a new line
    
    //initialization phase
    counter=1;//initialize loop counter
    
    //processing phase
    while (counter<=8){//loop 8 times
        
        printf("%s", "* ");//print asterick
        counter=counter+1;//increment counter
    }
    
    puts("");//outputs a new line
    printf("%s", " ");//outputs an extra space
    
    //initialization phase
    counter=1;//initialize loop counter
    
    //processing phase
    while (counter<=8){//loop 8 times
        
        printf("%s", "* ");//print asterick
        counter=counter+1;//increment counter
    }
    
    puts("");//outputs a new line

}//ends function main
